<%= link_to "Add new", new_todo_path %>

<ul class="todos">
    <% @todos.each do |todo| %>
        <li class="<%= "completed" if todo.completed? %>">  <!--- eu nem sabia que isso era possível -->
            <%= todo.title %>
            <%= button_to "Done", todo_completion_path(todo) %>
        </li>
    <% end %>
</ul>